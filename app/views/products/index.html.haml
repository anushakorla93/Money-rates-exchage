%h1 Check out our products!

.row.product-list
  - @products.each do |product|
    %article.product
      %h2= product.name
      %p= product.description
      %strong
        = product.price_in.exchange_to(product.currency)
        = product.currency

      = link_to 'Show more', product

